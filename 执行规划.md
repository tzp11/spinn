# SPINN 后续执行规划

## 1. 总体目标

本阶段的目标是在现有 SPINN 原型基础上，完成从“可运行推理框架”到“面向星载部署的可移植、全静态、可验证工具链”的闭环建设。

当前项目已经完成 ENNF 紧凑格式、ONNX 到 ENNF 转换、图优化、运行时加载/规划/执行、静态区间打包内存规划、算子库与多模型推理验证。后续工作的核心是补齐论文第三个创新点，并把前两个创新点从“已有实现与实验数据”推进到“可部署、可复现实验、可支撑论文结论”的状态。

### 1.1 核心交付目标

| 目标 | 说明 | 对应论文创新点 |
|:---|:---|:---|
| ENNF 到 C 源码转译器 | 将 `.ennf` 模型生成独立可编译的 `model.c/model.h` | 创新点 3 |
| 全静态内存推理 | Arena、权重、张量元数据尽可能编译期静态化，消除运行时动态分配 | 创新点 2/3 |
| M_max 内存上限约束 | 在规划阶段引入平台内存上限，提前判断模型是否可部署 | 创新点 2 |
| 纯 C 可移植路径 | 去除 AVX2/FMA 等平台强绑定，提供可交叉编译 fallback | 创新点 3 |
| 多平台验证实验 | x86、ARM、RISC-V/QEMU 或交叉编译验证 | 创新点 3 |
| 论文实验图表 | 补齐格式、内存、转译、交叉编译、正确性验证图表 | 全部 |

### 1.2 最终预期状态

完成后，工具链应支持以下流程：

```text
ONNX 模型
    ↓
onnx2ennf：格式转换 + 图融合 + 张量 ID 化
    ↓
ENNF 模型
    ↓
ennf2c：源码转译 + 静态内存规划 + 算子裁剪
    ↓
model.c / model.h / selected_ops
    ↓
目标平台 C 编译器交叉编译
    ↓
独立可执行推理程序
```

最终部署产物不再依赖 ONNX Runtime、Protobuf、动态模型解析器或完整算子注册表，而是一个仅包含当前模型所需算子的纯 C 推理程序。

---

## 2. 技术路线与技术选型

### 2.1 总体技术路线

采用“解释执行运行时 + 源码转译器”并行推进的路线：

1. 保留现有 SPINN runtime 作为参考实现与正确性基准。
2. 新增 `ennf2c` 工具，将 ENNF 模型离线转为 C 源码。
3. 逐步把 runtime 中的动态行为迁移到离线生成阶段：
   - 节点遍历顺序离线确定
   - 张量 offset 离线确定
   - 权重布局离线确定
   - 所需算子集合离线确定
   - 调度调用关系离线展开
4. 通过相同输入对比 runtime 输出与 generated C 输出，验证转译正确性。

### 2.2 编程语言选择

| 模块 | 技术选型 | 原因 |
|:---|:---|:---|
| `ennf2c` 转译器 | C | 复用现有 ENNF 结构定义，保持工具链零依赖，便于交叉编译 |
| 生成代码 | ISO C / C99 优先 | 最大化嵌入式与星载平台兼容性 |
| 实验脚本 | Python | 用于批量运行、结果统计、图表生成，不进入部署链路 |
| 构建系统 | Makefile | 与现有项目一致，简单、透明、适合交叉编译 |
| 验证基准 | 现有 SPINN runtime + ONNX Runtime | SPINN 用于端到端一致性，ORT 用于参考正确性 |

### 2.3 内存策略选择

| 内存对象 | 当前方式 | 目标方式 |
|:---|:---|:---|
| FeatureMap Arena | runtime `malloc` | 生成 `static uint8_t arena[PEAK]` |
| 权重数据 | ENNF 文件 lazy load + cache | `static const uint8_t weights[]` 或外部只读 bin |
| Tensor 元数据 | runtime 解析并分配 | 生成静态数组或直接展开为指针宏 |
| Node 参数 | runtime 解析并分配 | 生成静态参数结构体或直接内联常量 |
| 算子调度 | 函数指针表 | 直接函数调用 |

### 2.4 算子策略选择

短期不重写算子，而是复用现有 `run_time/ops/` 算子实现。

优先策略：

1. 建立“ENNF op_type → 算子 C 文件 → 调用函数”的映射表。
2. `ennf2c` 扫描模型节点，收集实际使用的算子集合。
3. 生成 Makefile 或编译清单，仅编译被使用的算子文件。
4. 第一阶段允许沿用现有算子接口，降低实现风险。
5. 第二阶段再逐步把复杂 runtime 上下文接口裁剪为更轻量的 generated model 接口。

---

## 3. 任务划分

## 阶段 A：M_max 内存上限约束实现

### A.1 目标

在现有静态内存规划器中增加平台内存上限参数，使工具链能够在部署前判断模型是否满足给定内存约束。

### A.2 主要任务

| 编号 | 任务 | 输出 |
|:---|:---|:---|
| A1 | 修改 `spinn_plan_memory` 接口，增加 `max_arena_bytes` 参数 | 新接口定义 |
| A2 | 将空闲链表初始块从无限上限改为 `max_arena_bytes` | 受限分配逻辑 |
| A3 | 分配失败时返回明确错误码 | 错误处理路径 |
| A4 | 输出诊断信息：所需大小、当前上限、失败张量 ID | 可解释错误报告 |
| A5 | 增加命令行参数，例如 `--max-arena=4M` | 可实验入口 |
| A6 | 对 MNIST、YOLOv10n、ResNet101 做 4MB/16MB/64MB/256MB 可行性实验 | 实验表格 |

### A.3 预期成功标准

- 无限制模式下结果与当前版本完全一致。
- 设置足够大的 `M_max` 时，模型正常运行。
- 设置过小的 `M_max` 时，规划阶段失败，而不是运行时崩溃。
- 错误信息能说明失败原因和所需内存规模。

### A.4 验证方式

| 验证项 | 方法 |
|:---|:---|
| 回归正确性 | 原有 MNIST/YOLOv10n/ResNet101 推理输出不变 |
| 边界测试 | `M_max = arena_peak - 1` 应失败，`M_max = arena_peak` 应成功 |
| 诊断信息 | 检查失败时是否输出 peak、limit、tensor id |
| 论文数据 | 形成不同内存约束下模型可部署性表格 |

---

## 阶段 B：ennf2c 转译器最小可用版本

### B.1 目标

实现一个最小可用的 `ennf2c`，能够读取 ENNF 文件并生成可编译的 C 工程骨架。

### B.2 主要任务

| 编号 | 任务 | 输出 |
|:---|:---|:---|
| B1 | 新建 `ennf2c.c`，复用 `ennf_def.h` 读取 Header、Tensor、Node、Weight Entry | ENNF 解析能力 |
| B2 | 生成 `model.h`：输入输出接口、Arena 大小、模型元信息 | 头文件 |
| B3 | 生成 `model.c`：静态 arena、权重数组、tensor 指针初始化、`model_run` 框架 | 源文件 |
| B4 | 生成 `generated/Makefile` | 可编译工程 |
| B5 | 先支持 MNIST 所需算子路径 | 最小闭环 |

### B.3 预期成功标准

- `ennf2c mnist.simplified.ennf generated/mnist/` 能生成完整文件。
- 生成代码可通过 gcc 编译。
- `model_run()` 能完成 MNIST 推理。
- 输出结果与 SPINN runtime 在误差阈值内一致。

### B.4 验证方式

| 验证项 | 方法 |
|:---|:---|
| 生成完整性 | 检查 `model.c/model.h/Makefile` 是否存在 |
| 编译验证 | `make` 能生成可执行文件或静态库 |
| 正确性验证 | 同输入下 generated C 与 SPINN runtime 输出最大误差 < 1e-4 |
| 算子裁剪验证 | 编译清单仅包含 MNIST 实际用到的算子 |

---

## 阶段 C：全静态内存与权重内嵌

### C.1 目标

将运行时推理过程中的动态分配迁移到编译期静态数组，实现 generated C 路径 0 次 `malloc/calloc/realloc/free`。

### C.2 主要任务

| 编号 | 任务 | 输出 |
|:---|:---|:---|
| C1 | 在 `ennf2c` 中调用或复刻内存规划逻辑，得到每个 FeatureMap offset | 静态 offset 表 |
| C2 | 生成 `static uint8_t arena[ARENA_SIZE]` | 静态 Arena |
| C3 | 将权重从 ENNF 中提取为 `static const uint8_t weights[]` 或独立 `weights.bin` | 只读权重存储 |
| C4 | 生成 tensor 指针宏，例如 `#define TENSOR_3 ((float*)(arena + offset))` | 零分配访问 |
| C5 | 移除 generated 路径中的 runtime context 动态构造 | 静态执行路径 |

### C.3 预期成功标准

- generated C 编译产物中不调用 `malloc/calloc/realloc/free`。
- Arena 大小等于内存规划得到的 peak。
- 权重地址为只读静态区域或外部只读文件映射。
- 推理输出与 runtime 一致。

### C.4 验证方式

| 验证项 | 方法 |
|:---|:---|
| 动态内存检查 | `nm`/源码 grep 确认无 malloc 系列符号 |
| Arena 检查 | generated header 中 `ARENA_SIZE` 与 profile_memory 结果一致 |
| 输出正确性 | generated C vs runtime 最大误差统计 |
| 星载论证 | 形成“动态分配次数：约 1012 次 → 0 次”的实验结论 |

---

## 阶段 D：算子裁剪与直接调用

### D.1 目标

消除运行时全量算子注册表和函数指针调度，让 generated C 只包含模型实际使用的算子，并按拓扑序直接调用。

### D.2 主要任务

| 编号 | 任务 | 输出 |
|:---|:---|:---|
| D1 | 建立 op_type 到算子源文件的映射 | `op_manifest` |
| D2 | 扫描 ENNF 节点集合，统计模型实际算子 | used ops 列表 |
| D3 | 生成只包含 used ops 的 Makefile | 裁剪构建系统 |
| D4 | `model_run` 中按节点顺序生成直接函数调用 | 直接调度代码 |
| D5 | 对比全量 runtime 与 generated C 二进制大小 | 体积实验 |

### D.3 预期成功标准

- generated C 不依赖 512 槽算子注册表。
- 编译产物只包含当前模型用到的算子文件。
- 二进制体积小于全量 `spinn_run`。
- 推理结果与 runtime 一致。

### D.4 验证方式

| 验证项 | 方法 |
|:---|:---|
| 算子数量 | 对比全量 213 个算子与模型 used ops 数 |
| 二进制大小 | `size`/`ls -lh` 统计 generated executable |
| 调度方式 | 源码中不出现函数指针 op registry 调度 |
| 性能趋势 | 记录直接调用版本与 runtime 的推理耗时 |

---

## 阶段 E：纯 C fallback 与跨平台编译

### E.1 目标

保证 generated C 能在不支持 AVX2/FMA 的目标上编译，满足星载平台可移植要求。

### E.2 主要任务

| 编号 | 任务 | 输出 |
|:---|:---|:---|
| E1 | 梳理当前算子中的 AVX2/FMA 依赖 | 平台依赖清单 |
| E2 | 为关键算子增加纯 C fallback | portable ops |
| E3 | 增加编译宏，例如 `SPINN_USE_AVX2`、`SPINN_PORTABLE_C` | 可配置后端 |
| E4 | 使用 aarch64/riscv64/arm-none-eabi 工具链进行交叉编译 | 编译结果 |
| E5 | 若可行，使用 QEMU 运行小模型验证 | 模拟器验证 |

### E.3 预期成功标准

- x86 关闭 AVX2 后仍可编译运行。
- ARM/RISC-V 工具链能编译 generated C。
- QEMU 或目标模拟环境中能运行 MNIST generated 模型。
- 形成多架构二进制体积对比表。

### E.4 验证方式

| 验证项 | 方法 |
|:---|:---|
| 可移植编译 | `gcc`, `aarch64-linux-gnu-gcc`, `riscv64-linux-gnu-gcc` 编译 |
| SIMD 隔离 | portable 模式不包含 AVX intrinsics |
| 运行验证 | QEMU 中运行 MNIST，对比输出 |
| 论文图表 | 多目标架构编译结果表 |

---

## 阶段 F：实验、图表与论文支撑材料

### F.1 目标

把工程结果转化为论文可用的数据、图表和结论。

### F.2 必要实验

| 实验 | 指标 | 目标结论 |
|:---|:---|:---|
| ONNX vs ENNF 格式对比 | 元数据大小、字符串占比、解析代码大小 | ENNF 适合受限平台 |
| 内存规划效率 | ΣFeatureMap、Arena Peak、节省率 | 区间打包显著降低内存峰值 |
| 融合前后对比 | 节点数、张量数、Arena Peak | 图融合对内存有级联收益 |
| M_max 约束实验 | 成功/失败、所需 peak、平台限制 | 可部署性可在编译/规划阶段判断 |
| Runtime vs Generated C | 二进制大小、动态分配次数、调度方式 | 源码转译更适合星载部署 |
| 多架构交叉编译 | 编译是否成功、二进制大小 | 工具链具备异构可移植性 |
| 正确性验证 | 最大误差、Top-1/输出一致性 | 转译不改变计算语义 |

### F.3 需要生成的图表

| 图表 | 用途 |
|:---|:---|
| ONNX 字符串引用 vs ENNF 整数 ID 引用示意图 | 支撑创新点 1 |
| ONNX/ENNF 文件组成堆叠柱状图 | 支撑格式紧凑性 |
| 内存生命周期区间图 | 支撑区间打包算法 |
| 朴素分配 vs 区间打包 vs 融合+区间打包 对比图 | 支撑创新点 2 |
| ONNX → ENNF → C → 交叉编译工具链流程图 | 支撑创新点 3 |
| Runtime vs Generated C 二进制体积对比图 | 支撑源码转译效果 |
| 多平台编译结果表 | 支撑可移植性 |

---

## 4. 里程碑计划

### 第 1 周：M_max 与 ennf2c 骨架

| 日期范围 | 任务 | 产出 |
|:---|:---|:---|
| Day 1-2 | 实现 M_max 约束 | 受限内存规划器 |
| Day 3 | 完成 M_max 回归测试 | 可行性实验初版 |
| Day 4-5 | 搭建 `ennf2c` 解析与代码生成骨架 | 可生成 `model.c/model.h` |
| Day 6-7 | MNIST 最小闭环 | generated MNIST 可编译 |

### 第 2 周：全静态内存与正确性闭环

| 日期范围 | 任务 | 产出 |
|:---|:---|:---|
| Day 8-9 | 静态 Arena + offset 生成 | 0 malloc Arena |
| Day 10-11 | 权重内嵌与 tensor 指针生成 | 完整静态模型数据 |
| Day 12-13 | MNIST 输出对齐 runtime | 正确性报告 |
| Day 14 | 整理阶段实验数据 | 第一次论文数据更新 |

### 第 3 周：算子裁剪与中大型模型扩展

| 日期范围 | 任务 | 产出 |
|:---|:---|:---|
| Day 15-16 | op manifest 与 used ops 收集 | 算子裁剪机制 |
| Day 17-18 | 直接调用生成 | 去除 registry 调度 |
| Day 19-20 | 扩展到 YOLOv10n/ResNet101 关键路径 | 中大型模型 generated 初版 |
| Day 21 | 二进制大小与动态分配统计 | 对比实验表 |

### 第 4 周：可移植与论文材料收敛

| 日期范围 | 任务 | 产出 |
|:---|:---|:---|
| Day 22-24 | 纯 C fallback 与 SIMD 隔离 | portable 模式 |
| Day 25-26 | ARM/RISC-V 交叉编译/QEMU 验证 | 多平台结果 |
| Day 27-28 | 生成图表与论文段落 | 实验章节材料 |

---

## 5. 成功标准

### 5.1 工程成功标准

| 标准 | 目标值 |
|:---|:---|
| `ennf2c` 可用性 | 至少支持 MNIST 完整源码转译与运行；尽量扩展到 YOLOv10n/ResNet101 |
| 动态内存 | generated C 推理路径 0 次 malloc/calloc/realloc/free |
| 算子裁剪 | generated C 只编译模型实际使用算子 |
| 正确性 | generated C 与 SPINN runtime 输出误差在可接受范围内 |
| 可移植性 | 至少完成 x86 + 一个非 x86 目标的交叉编译 |
| 可复现性 | 每个实验有命令、输入模型、输出日志、统计脚本 |

### 5.2 论文成功标准

| 创新点 | 必须支撑的数据 |
|:---|:---|
| ENNF 紧凑格式 | ONNX/ENNF 元数据大小、字符串占比、解析代码体积 |
| 融合与静态内存规划 | FeatureMap Σsize、Arena Peak、节省率、融合前后对比 |
| C 源码转译 | 二进制大小、动态分配次数、算子数量、交叉编译结果、输出正确性 |

### 5.3 最低可接受成果

如果时间不足，最低目标为：

1. `M_max` 约束完成。
2. `ennf2c` 支持 MNIST 完整生成、编译、运行。
3. generated C 实现静态 Arena 与权重内嵌。
4. generated C 推理路径无动态内存分配。
5. 至少完成 x86 与一个交叉编译目标的编译验证。
6. 形成可写入论文的创新点 3 初步实验表。

---

## 6. 风险与应对策略

| 风险 | 影响 | 应对策略 |
|:---|:---|:---|
| 算子接口与 generated C 不匹配 | ennf2c 实现复杂度上升 | 第一阶段复用 runtime 算子接口，第二阶段再裁剪 |
| YOLOv10n/ResNet101 算子路径复杂 | 中大型模型转译延期 | 先保证 MNIST 闭环，再逐步扩展 used ops |
| AVX2 依赖过重 | 无法交叉编译 | 增加 portable C fallback，使用编译宏隔离 SIMD |
| 权重内嵌导致 C 文件过大 | 编译时间长、源码不可读 | 支持两种模式：内嵌数组 / 外部只读 bin |
| QEMU 运行环境配置耗时 | 影响验证进度 | 最低要求先完成交叉编译，QEMU 作为增强验证 |
| 输出误差难以对齐 | 正确性验证受阻 | 先逐层 dump 对比，定位到具体算子 |

---

## 7. 推荐优先级

### 必须先做

1. `M_max` 内存上限约束。
2. `ennf2c` 最小骨架。
3. MNIST generated C 闭环。
4. 全静态 Arena 与 0 malloc 验证。

### 随后推进

1. 算子裁剪与直接调用。
2. YOLOv10n/ResNet101 generated C 扩展。
3. 纯 C fallback。
4. 交叉编译与 QEMU。

### 最后补充

1. INT8/INT16 定点化。
2. SEU 检测。
3. 功耗测量。

---

## 8. 当前下一步建议

建议立即从阶段 A 开始：先实现 `M_max` 约束，因为它改动小、收益明确、能直接补强创新点 2。随后进入阶段 B，搭建 `ennf2c` 最小闭环。这样可以在一周内得到新的可展示成果，并降低后续源码转译实现的不确定性。
