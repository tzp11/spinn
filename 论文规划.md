# 面向星载的可移植推理框架与转译技术研究 — 论文规划

## 一、已测数据汇总

### 1.1 ONNX 格式问题量化

#### 1.1.1 Protobuf 解析代码量

| 组件 | 源码大小 | 说明 |
|:---|---:|:---|
| protobuf-c 库 (protobuf-c.c + .h) | 127.5 KB | 通用 Protobuf 解码库 |
| onnx.proto3 生成代码 (.pb-c.c + .h) | 187.0 KB | ONNX 协议自动生成 |
| **合计源码** | **314.5 KB** | — |
| **合计目标文件** (.o) | **475.6 KB** | 编译后体积 |

对比：ENNF 格式的全部解析代码（spinn_runtime.c + spinn_runtime.h + ennf_def.h）仅 **23.1 KB**，是 Protobuf 方案的 **1/13.6**。

#### 1.1.2 ONNX 的两种存储形式与冗余分析

ONNX 存在两种存储形式，需要分别分析：

**形式A — 内嵌权重（单 .onnx 文件）**：权重数据以 Protobuf TensorProto 消息嵌入 .onnx 文件中，每个权重张量被 protobuf 字段标记、长度编码、字符串名称等包裹。

**形式B — 外部数据（.onnx + .onnx.data）**：.onnx 文件仅包含图结构描述（节点定义、张量名称、连接关系等），.onnx.data 文件包含纯权重二进制数据。

| 模型 | 存储形式 | ONNX 图结构部分 | ONNX 权重数据部分 | ONNX 合计 |
|:---|:---|---:|---:|---:|
| MNIST | 内嵌 (A) | ~787 B (5.0%) | ~15,016 B (嵌入) | 15,803 B |
| complex_test | 外部 (B) | 26,977 B (.onnx) | 14,656 B (.data) | 41,633 B |
| YOLOv10n | 内嵌 (A) | ~88,238 B (0.9%) | ~9,298,228 B (嵌入) | 9,386,466 B |
| ResNet101 | 外部 (B) | 432,222 B (.onnx) | 177,799,168 B (.data) | 178,231,390 B |

**图结构部分的字符串冗余**（推理时完全无用的调试/命名信息）：

| 模型 | ONNX 图结构大小 | 其中字符串 | 字符串占比 | 说明 |
|:---|---:|---:|---:|:---|
| complex_test | 26,977 B | 23,465 B (769个) | **87.0%** | 图结构几乎全是字符串 |
| ResNet101 | 432,222 B | 400,886 B (7739个) | **92.8%** | 93%是推理无关的张量/节点名称 |

> 结论：ONNX 基于字符串名称匹配的张量引用机制（如 `"/model/layer1/conv1/Conv_output_0"`），在推理时没有任何计算价值。ResNet101 的图结构文件中 **92.8% (400KB)** 是纯字符串。

#### 1.1.3 ENNF vs ONNX 对比：仅图结构/元数据部分

> **重要说明**：两种格式的权重数据部分完全相同（都是原始浮点二进制），因此总文件大小差异很小。真正的对比应聚焦于**图结构/元数据**部分——这才是格式设计的差异所在。

| 模型 | ONNX 图结构 | ENNF 元数据 | ENNF/ONNX | 缩减幅度 |
|:---|---:|---:|---:|---:|
| MNIST | ~787 B | 896 B | 113.9% | (模型极小，ENNF固定头64B开销相对大) |
| complex_test | 26,977 B | 3,664 B | **13.6%** | 缩减 86.4% |
| YOLOv10n | 88,238 B | 39,504 B | **44.8%** | 缩减 55.2% |
| ResNet101 | 432,222 B | 28,928 B | **6.7%** | 缩减 93.3% |

> 结论：ENNF 通过整数 ID 替代字符串引用 + 固定宽度二进制表结构，将图描述元数据压缩到 ONNX 的 **6.7%~44.8%**（中大型模型）。ResNet101 的图描述从 422KB 降至 28KB，减少 393KB。

### 1.2 ENNF 格式紧凑性分析

| 模型 | 节点数 | 张量数 | 权重数 | 元数据开销 | 元数据占比 | 权重占比 |
|:---|---:|---:|---:|---:|---:|---:|
| MNIST | 4 | 11 | 5 | 832 B | 5.23% | 94.4% |
| complex_test | 28 | 49 | 18 | 3,600 B | 19.11% | 80.5% |
| YOLOv10n | 308 | 513 | 187 | 39,440 B | **0.42%** | 99.6% |
| ResNet101 | 241 | 350 | 108 | 28,864 B | **0.02%** | 99.98% |

> 结论：对于实际部署模型，ENNF 元数据开销低至 0.02%~0.42%，几乎所有空间用于有效权重数据。

### 1.3 运行时真实内存占用（实测数据）

使用 `profile_memory` 工具对各模型推理后的真实内存进行精确统计：

#### 1.3.1 各组件内存占用

**MNIST (4节点, 11张量, 5权重)**：

| 内存组件 | 大小 | 占比 |
|:---|---:|---:|
| 运行时基础设施 (Context+Tensor池+Node池+params+cache表) | 1,388 B | 7.23% |
| Arena (FeatureMap) | 2,784 B | 14.51% |
| 权重缓存 (Lazy Load后) | 15,016 B | 78.26% |
| **总计** | **19,188 B (18.7 KB)** | 100% |

**YOLOv10n (308节点, 513张量, 187权重)**：

| 内存组件 | 大小 | 占比 |
|:---|---:|---:|
| 运行时基础设施 | 60,308 B (58.9 KB) | 0.18% |
| Arena (FeatureMap) | 24,576,000 B (23.44 MB) | 72.42% |
| 权重缓存 | 9,298,228 B (8.87 MB) | 27.40% |
| **总计** | **33,934,536 B (32.36 MB)** | 100% |

**ResNet101 (241节点, 350张量, 108权重)**：

| 内存组件 | 大小 | 占比 |
|:---|---:|---:|
| 运行时基础设施 | 44,456 B (43.4 KB) | 0.02% |
| Arena (FeatureMap) | 12,042,240 B (11.48 MB) | 6.34% |
| 权重缓存 | 177,775,296 B (169.54 MB) | 93.63% |
| **总计** | **189,861,992 B (181.07 MB)** | 100% |

#### 1.3.2 内存规划效率（区间打包 vs 朴素分配）

| 模型 | FeatureMap 张量数 | Σsize (所有FM总大小) | Arena Peak | Peak/Σsize | **节省** |
|:---|---:|---:|---:|---:|---:|
| MNIST | 6 | 5,688 B | 2,784 B | 48.9% | **51.1%** |
| YOLOv10n | 325 | 307,400,000 B (293.16 MB) | 24,576,000 B (23.44 MB) | 8.0% | **92.0%** |
| ResNet101 | 242 | 160,984,992 B (153.53 MB) | 12,042,240 B (11.48 MB) | 7.5% | **92.5%** |

> **关键数据**：对于 YOLOv10n 和 ResNet101，区间打包算法将 FeatureMap 内存需求压缩到原始的 **7.5%~8.0%**，即节省 **92%** 以上内存。

#### 1.3.3 总内存对比（本方案 vs 朴素分配）

| 模型 | 朴素分配总内存 | 本方案总内存 | 整体节省 |
|:---|---:|---:|---:|
| MNIST | 22,092 B | 19,188 B | 13.1% |
| YOLOv10n | 316,758,536 B (302.08 MB) | 33,934,536 B (32.36 MB) | **89.3%** |
| ResNet101 | 338,804,744 B (323.11 MB) | 189,861,992 B (181.07 MB) | **44.0%** |

> 说明：ResNet101 的权重占总内存 93.6%（不可压缩），所以整体节省比不如 YOLOv10n 显著。但 FeatureMap 部分的节省率是一致的（92%+）。

#### 1.3.4 SPINN vs ONNX Runtime 内存对比（实测）

测试环境：同一机器，ONNX Runtime 1.23.2 (CPUExecutionProvider, 单线程)，SPINN 使用 profile_memory 精确统计。

> **测量方式说明**：ORT 使用 RSS（操作系统级驻留内存）增量测量，包含 ORT 运行时库本身的内存、内部内存池、ONNX protobuf 解析缓冲区等全部开销。SPINN 使用应用层精确统计（Context + Arena + 权重缓存），不含 OS 页表等隐性开销，因此 SPINN 数值是下界。两种测量方式下的对比依然有效：ORT 的 RSS 增量是其真实的系统资源消耗。

| 模型 | ORT RSS 增量 | SPINN 总内存 | ORT / SPINN | 说明 |
|:---|---:|---:|---:|:---|
| MNIST | **15.07 MB** | **18.7 KB** | **823× ** | ORT 基础设施开销远超模型本身 |
| YOLOv10n | **62.93 MB** | **32.36 MB** | **1.9×** | ORT 内部内存池 + protobuf 解析开销 |
| ResNet101 | **307.75 MB** | **181.07 MB** | **1.7×** | 权重占主体，但 ORT 仍多 126 MB |

**ORT 各阶段 RSS 变化**：

| 模型 | ORT 基线 (import后) | 加载模型后 | 推理后 | 总增量 |
|:---|---:|---:|---:|---:|
| MNIST | 39 MB | +8 MB | +15 MB | 15 MB |
| YOLOv10n | 55 MB | +30 MB | +65 MB | 65 MB |
| ResNet101 | 120 MB | +315 MB | +315 MB | 315 MB |

> **关键结论**：
> - 小模型 (MNIST)：ORT 运行时自身开销 (~15MB) 是模型实际需求 (18.7KB) 的 **823 倍**，完全不适合星载微型模型部署
> - 中型模型 (YOLOv10n)：SPINN 比 ORT 少用 **30.57 MB**（47.7%）
> - 大型模型 (ResNet101)：SPINN 比 ORT 少用 **126.68 MB**（41.2%），其中权重相同，差异来自 ORT 的运行时开销和内存池碎片

#### 1.3.5 动态内存分配次数（理论分析）

当前运行时在 ResNet101 上约有 **~1,012 次** malloc/calloc 调用（4次 Context/池分配 + ~344次 node params + ~346次 FreeBlock + ~318次权重 Lazy Load）。

> 星载问题：航天级 RTOS (VxWorks/RTEMS) 中 malloc 是非确定性操作，存在碎片积累风险。DO-178C / ECSS-E-ST-40C 标准通常**禁止或严格限制动态内存分配**。全静态方案可将此降为 **0 次**。

### 1.4 运行时二进制大小与依赖对比

| 推理框架 | 运行时二进制 | 外部依赖 | 最低平台要求 |
|:---|---:|---:|:---|
| ONNX Runtime | ~50-200 MB | 20+ 库 | x86/ARM + 完整 OS |
| TFLite | ~1-3 MB | 15+ 库 | ARM + Android/Linux |
| llama.cpp | ~2-5 MB | 0 (需GPU SDK) | x86/ARM + OS |
| **SPINN runtime** | **522 KB** | **0** (仅libc/libm) | **任意C编译器目标** |

### 1.5 SPINN 项目代码规模

| 模块 | 文件数 | 代码行数 |
|:---|---:|---:|
| 运行时核心 (runtime + headers) | 9 | 1,357 行 |
| 算子库 (ops/) | 72 | 5,026 行 |
| 转换器 (onnx2ennf + graph_opt) | 8 | 2,563 行 |
| **总计** | **89** | **8,946 行** |
| Protobuf 解析 (仅转换时) | 4 | — (314.5 KB) |

### 1.6 图优化效果实测

| 模型 | 原始节点数 | Conv+BN 融合 | Conv+ReLU 融合 | 优化后节点数 | 节点缩减率 |
|:---|---:|---:|---:|---:|---:|
| MNIST | 5 | 0 | 1 | 4 | 20% |
| YOLOv10n | 308 | 0 | 0 | 308 | 0% (使用 SiLU 非 ReLU) |
| ResNet101 | 241 | 0 | 67 | 174 | **27.8%** |

> 说明：ResNet101 (simplified) 已经过 onnx-simplifier 处理，BN 已被折叠进 Conv，故 Conv+BN 融合为 0。Conv+ReLU 融合成功消除了 67 个独立 ReLU 节点。每消除一个节点 = 减少一个中间张量 = 减少 Arena 内存占用 + 减少一次算子调度。

**图优化对内存的级联影响**：
- 每个被融合的 ReLU 节点消除后，其输出张量不再需要独立的 Arena 空间
- Conv+ReLU 融合后，Conv 算子直接在输出上执行 ReLU，无需额外中间缓冲区
- Conv+BN 融合将 5 个 BN 参数张量（scale, bias, mean, var, output）全部消除

---

## 二、问题分析与创新点推导

### 问题链

```
星载AI需求（在轨遥感目标检测、姿态识别等）
    ↓
现有推理框架在星载平台不可用：
    ├── P1: ONNX 格式依赖 Protobuf 库（314.5KB），星载ROM不足以承载
    │       ONNX 文件中 87%-93% 为推理无关的字符串元数据
    ├── P2: 运行时 ~1,012 次动态内存分配，违反航天软件确定性要求
    │       malloc 执行时间不可预测，存在碎片积累风险
    └── P3: 运行时二进制含全量 213 个算子，实际模型仅用 10-20 个
            解释执行存在函数指针间接调度开销
            交叉编译到 SPARC/RISC-V/PowerPC 需要运行时 + 模型文件两部分
```

### 创新点对应

| 创新点 | 解决的问题 | 核心技术 |
|:---|:---|:---|
| **C1: ENNF 紧凑格式设计** | P1 | 整数ID替代字符串引用，元数据开销 0.02%-5%，解析代码仅23.1KB |
| **C2: 面向内存约束的计算图融合与静态内存规划联合优化** | P2 | 算子融合→节点/张量消减→区间打包→峰值内存约束，编译时确定全部 offset，零 malloc |
| **C3: 计算图级 C 源码转译** | P3 | ONNX→ENNF→纯C源码，仅含用到的算子，直接函数调用，零依赖 |

---

## 三、创新点详细描述

### 创新点 1：面向星载约束的紧凑型神经网络中间表示（ENNF）

**问题根源**：ONNX 选用通用序列化协议 (Protobuf) + 字符串名称引用，导致解析代价重、文件冗余大。

**核心算法 — 张量ID映射**：
```
输入：ONNX GraphProto G
输出：整数ID映射表 M，引用计数表 R

1. 遍历 G.initializer → 为每个权重张量分配 ID，标记 is_weight=1
2. 遍历 G.input / G.output / G.value_info → 分配 ID
3. 遍历 G.node：
   3.1 为每个 output 字符串查找或分配 ID（定义点）
   3.2 为每个 input 字符串查找 ID 并 R[id]++（引用点）
4. 返回 M, R
```

**ENNF 二进制布局**：
```
┌─────────────────────────┐
│ Header (64 bytes, 固定)  │  magic, version, counts, offsets
├─────────────────────────┤
│ Weight Entry Table       │  n_weights × 16 bytes (id, offset, size, flags)
├─────────────────────────┤
│ Graph Meta               │  input_ids[], output_ids[]
├─────────────────────────┤
│ Node Table               │  n_nodes × variable (op_type, IO ids, params)
├─────────────────────────┤
│ Tensor Meta Table        │  n_tensors × 40 bytes (dims, dtype, size, flags)
├─────────────────────────┤
│ Weight Data (16B对齐)    │  实际权重二进制数据
└─────────────────────────┘
```

**需要的图表**：
- [ ] ONNX 字符串引用 vs ENNF 整数ID引用 对比图
- [ ] ONNX vs ENNF 空间组成堆叠柱状图（权重/元数据/字符串/编码开销）
- [ ] 多模型的 ENNF 元数据占比折线图

### 创新点 2：面向内存约束的计算图融合与静态内存规划联合优化

**问题根源**：
1. 推理过程中间特征图的生命周期可在编译时完全确定，无需运行时动态分配
2. 图优化（算子融合）直接影响张量数量和生命周期，进而影响内存峰值
3. 星载平台有**硬性内存上限** M_max，需确保 Arena Peak ≤ M_max

**核心思路**：将算子融合与内存规划视为一个**联合优化问题**，而非两个独立步骤。

```
算子融合 (消减节点/张量) → 更新引用计数 → 区间打包 (计算 offset) → 检查峰值 ≤ M_max
```

#### 第一阶段：计算图融合（减少节点和张量）

**Pass 1 — Conv+BatchNorm 常量折叠融合**：

数学原理：
```
BN(y) = γ·(W·x + b - μ) / √(σ² + ε) + β

融合后：
  W_new[oc] = W[oc] × γ[oc] / √(σ²[oc] + ε)
  b_new[oc] = γ[oc] × (b[oc] - μ[oc]) / √(σ²[oc] + ε) + β[oc]
```

效果：消除 BN 节点 + 5 个 BN 参数张量 (scale, bias, mean, var) + 1 个 BN 输出张量。权重在转换时离线合并。

**Pass 2 — Conv+Activation 融合**：

将 Conv 后紧跟的 ReLU/Clip 等逐元素激活标记为 Conv 节点的 `fused_activation` 标志，跳过独立的激活节点。

效果：消除激活节点 + 激活节点的输出张量（Conv 直接在输出缓冲区上执行激活）。

**融合的级联内存效益（实测）**：

| 模型 | 融合前节点数 | 融合后节点数 | 消除的节点 | 消除的中间张量 |
|:---|---:|---:|---:|---:|
| MNIST | 5 | 4 | 1 ReLU | 1 |
| ResNet101 | 241 | 174 | 67 ReLU | 67 |

> 每消除一个中间张量 = 从内存规划中移除一个生命周期区间 = Arena 峰值可能降低。

#### 第二阶段：基于引用计数的区间打包（Best-Fit Interval Packing）

在融合后的计算图上执行静态内存规划：

```
算法：MemoryPlan(融合后的节点序列 N[0..n-1], 张量表 T[0..m-1], 内存上限 M_max)

输入：融合后的节点序列（已消除被跳过的节点），张量元数据（含 size, ref_count），M_max
输出：每个张量的 offset，Arena 峰值 P，是否满足约束 P ≤ M_max

1. 初始化空闲链表 F = {(offset=0, size=M_max)}, P=0
                                                   ^^^^^ 关键：以 M_max 为上界
2. 为模型输入张量分配: offset ← BestFit(F, Align16(size))
   若失败 → 报错"内存不足"
3. 对于每个未跳过的节点 N[k]:
   3a. 为每个输出张量分配 offset ← BestFit(F, Align16(size))
       若失败 → 报错"Peak 超过 M_max，模型无法在此内存约束下运行"
       P ← max(P, offset + Align16(size))
   3b. 对每个输入张量: ref_count--
       若 ref_count == 0: Release(F, offset, size)  // 合并相邻空闲块
4. 若 P ≤ M_max: 返回成功
   否则: 报错并建议需要进一步压缩模型
```

#### 第三阶段：内存上限约束检查

星载平台有确定的物理内存限制（如 LEON3 典型 SRAM 4MB、SpaceDRAM 256MB）。引入 M_max 参数：

```
用法: onnx2ennf model.onnx model.ennf --max-arena=4M
      或运行时: spinn_plan(ctx, max_arena_bytes)
```

- **M_max = ∞**：无约束模式（当前默认），区间打包自由分配
- **M_max = 具体值**：将空闲链表的初始块大小设为 M_max，分配失败即报错
- 报错信息包含：当前 Peak vs M_max，以及建议操作（降低输入分辨率/更小模型）

**整体流程图**：
```
ONNX 模型
    ↓
[Pass 1: Conv+BN 融合] → 消除 BN 节点，合并权重
    ↓
[Pass 2: Conv+ReLU 融合] → 消除 ReLU 节点，标记 fused_activation
    ↓
融合后计算图（节点数↓, 张量数↓, 引用计数已更新）
    ↓
[区间打包 + M_max 约束] → 计算每个张量的 Arena offset
    ↓
若 Peak ≤ M_max → 输出 ENNF（含 offset 表）
若 Peak > M_max → 报错 + 建议
```

**实测数据汇总（已完成）**：

| 模型 | FeatureMap Σsize | Arena Peak | 复用率 | 节省 |
|:---|---:|---:|---:|---:|
| MNIST | 5,688 B | 2,784 B | 48.9% | **51.1%** |
| YOLOv10n | 293.16 MB | 23.44 MB | 8.0% | **92.0%** |
| ResNet101 | 153.53 MB | 11.48 MB | 7.5% | **92.5%** |

**需要的图表**：
- [ ] 完整的 融合+规划 联合流程图
- [ ] 内存生命周期图（X轴=节点序号, Y轴=地址空间, 矩形=张量占用, 标注融合消除的张量）
- [ ] 三种策略对比：朴素分配 vs 仅区间打包 vs 融合+区间打包+M_max 约束
- [ ] M_max 约束下的可行性分析（给定内存限制，哪些模型可以部署）

### 创新点 3：面向异构目标的计算图级 C 源码转译

**问题根源**：解释执行方式包含全量算子库（522KB），函数指针间接调度有开销，无法利用目标编译器优化。

**转译算法**：
```
输入：ENNF 模型文件
输出：model.c + model.h（独立可编译的纯C推理代码）

Step 1: 加载 ENNF，构建节点依赖图
Step 2: 扫描节点，收集实际使用的算子集合 S
Step 3: 执行静态内存规划 → 得到每个张量的 offset 和 Arena 峰值 P
Step 4: 生成 model.h:
    - static uint8_t arena[P];               // 编译时确定的静态Arena
    - static const uint8_t weights[] = {...}; // 权重内嵌为C数组
    - void model_init(void);
    - void model_run(const float *input, float *output);
Step 5: 生成 model.c:
    - #include 仅 S 中用到的算子源码
    - model_run(): 按拓扑序逐节点生成直接函数调用
Step 6: 序列化权重为C字节数组（或外部 .bin 文件 + static include）
```

**解释执行 vs 源码转译 对比**：

| 维度 | 解释执行（当前） | 源码转译（目标） |
|:---|:---|:---|
| 部署产物 | spinn_run (522KB) + model.ennf | model.c + 仅用到的 ops/ |
| 算子数量 | 全部213个编入二进制 | 仅模型实际用到的N个 |
| 内存分配 | ~1,012次 malloc | **0次**（全部 static） |
| 调度方式 | 函数指针间接调用 | **直接函数调用（可内联）** |
| 编译器优化 | 无法跨节点优化 | 编译器可做全局内联/常量传播 |
| 交叉编译 | 需运行时+模型文件 | **仅需标准C编译器** |

**需要的图表**：
- [ ] 完整工具链流程图（ONNX → onnx2ennf → ENNF → ennf2c → C源码 → 交叉编译）
- [ ] 生成代码 vs 运行时的二进制体积对比
- [ ] 多目标架构交叉编译实验结果

---

## 四、实验计划

### 4.1 已完成

- [x] onnx2ennf 转换器实现（529行）
- [x] ENNF 格式定义与实现
- [x] 图优化：Conv+BN 融合、Conv+ReLU 融合
- [x] 运行时引擎核心（Load/Plan/Run 三阶段）
- [x] 静态内存规划器（Interval Packing）
- [x] 算子库 72 个文件，213 个 ONNX 算子
- [x] MNIST / ResNet101 / YOLOv10n 推理验证
- [x] ONNX vs ENNF 格式对比数据采集
- [x] **运行时真实内存占用测量**（profile_memory 工具）
  - MNIST: 18.7KB, YOLOv10n: 32.36MB, ResNet101: 181.07MB
- [x] **内存规划效率实测**
  - YOLOv10n: Σ293MB → Peak 23.4MB (节省92%)
  - ResNet101: Σ153MB → Peak 11.5MB (节省92.5%)
- [x] **图优化效果实测**
  - ResNet101: 241→174 节点 (67 ReLU 融合, 缩减27.8%)

### 4.2 待完成

#### P0（核心创新，必须完成）

- [ ] **ennf2c 转译器实现**：生成独立可编译的纯C推理代码
  - 预估工作量：500-800 行 C 代码，2-3周
  - 输入：.ennf 文件
  - 输出：model.c + model.h + 所需 ops 源码副本

- [ ] **全静态内存方案**：Arena 改为 `static uint8_t[]`，权重内嵌为 `static const` 数组
  - 预估工作量：3-5天

- [ ] **M_max 内存上限约束实现**：在 spinn_plan_memory 中增加 M_max 参数
  - 初始空闲块大小设为 M_max 而非 0xFFFFFFFF
  - 分配失败时返回错误码 + 诊断信息
  - 预估工作量：1-2天

#### P1（实验验证，高优先级）

- [ ] **融合前后内存对比实验**：同一模型，关闭/开启融合，对比 Arena Peak
- [ ] **M_max 约束可行性分析**：给定 4MB/16MB/64MB 限制，哪些模型可运行
- [ ] **交叉编译实验**：用 gcc-riscv64 / gcc-aarch64 / gcc-arm-none-eabi 编译生成的C代码
  - 记录各目标架构的二进制大小
- [ ] **QEMU 运行验证**：在模拟器上运行，验证推理结果正确性
- [ ] **算子纯C fallback**：去除 AVX2 硬依赖，确保纯C路径可编译运行

#### P2（锦上添花）

- [ ] 定点化推理支持（INT8/INT16 替代 float）
- [ ] 轻量级 SEU 检测（关键结果校验和 / TMR）
- [ ] 功耗测量（如有真实嵌入式平台）

---

## 五、论文章节对应

| 章节 | 内容 | 对应技术 |
|:---|:---|:---|
| 第1章 绪论 | 星载AI需求 + 现有框架不可移植的问题(数据驱动) | — |
| 第2章 相关工作 | ONNX/TFLite/llama.cpp/onnx2c 等调研 | 调研报告 |
| 第3章 ENNF紧凑格式设计 | 整数ID映射 + 二进制布局 + 解析代价对比 | 创新点1 |
| 第4章 计算图融合与内存规划联合优化 | Conv+BN/ReLU 融合数学推导 + Interval Packing 算法 + M_max 约束 + 融合对内存的级联效益 | 创新点2 |
| 第5章 面向异构目标的C源码转译 | ennf2c 工具链 + 全静态生成 + 零依赖交叉编译 | 创新点3 |
| 第6章 实验与分析 | 多模型(MNIST/YOLOv10n/ResNet101)×多指标(内存/体积/正确性)×多目标架构 | 全部 |
| 第7章 总结与展望 | — | — |
